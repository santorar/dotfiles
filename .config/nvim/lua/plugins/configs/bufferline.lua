require('bufferline').setup{
    options = {
        mode = 'buffers',
        numbers = 'none',
        max_name_length = 15,
        max_prefix_length = 10,
        tab_size = 15,
        diagnostics = 'nvim_lsp',
    }
}
